import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const primary = Color(0xff0861dd);
  static const primaryDark = Color(0xFF1E3A8A);

  // Secandry
  static const secandrylight = Color(0xffacd6ff);
  static const secandrydark = Color(0xffcbd4ff);

  // Background
  static const bgLight = Colors.white;
  static const bgDark = Color(0xFF0F172A);

  // Surface
  static const surfaceLight = Colors.white;
  static const surfaceDark = Color(0xFF1E293B);

  // Text
  static const textLight = Color(0xFF111827);
  static const textDark = Color(0xFFE5E7EB);

  // Status
  static const success = Color(0xFF22C55E);
  static const error = Color(0xFFEF4444);
}
